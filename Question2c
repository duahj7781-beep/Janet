#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	float voltage;
	float safeLimit =11.5;
	
	printf("Enter measured voltage:");
	scanf ("%f", &voltage);
	
	if (voltage> safeLimit)
	printf("Warning");
	return 0;
}
